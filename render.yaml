services:
  - type: web
    name: abcdef-card-game
    runtime: node
    plan: free
    branch: claude/abcdef-card-game-kay002
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /api/cards
